// This is a test program which can be compiled by the TastierCompiler.
// It creates a constant value and several ints, writes them and also
// tests the ability of "write" to modify multiple values.

program Test {
  const a := 11;
  int i;
  int b;
  
  void SumUp() {
    int sum;
    sum := 0;
    while (i > 0) { sum := sum + i; i := i - 1; }
    write sum;
  }

  void Main() {
    
    b := a + 1;
    
    write a;
    write b, 99, 100;
  
  while (i > 0) {
      SumUp();
    }
  }
}

